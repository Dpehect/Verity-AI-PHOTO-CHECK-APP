export default function Loading() {
  return (
    <main className="route-state" aria-live="polite">
      <span className="route-state__scan" />
      <p>VERITY / LOADING</p>
      <h1>Preparing evidence.</h1>
    </main>
  );
}
