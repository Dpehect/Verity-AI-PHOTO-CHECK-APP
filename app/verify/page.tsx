import { VerificationLab } from "@/components/verification-lab";

export const metadata = { title: "Verify an asset — Verity" };

export default function VerifyPage() {
  return <VerificationLab />;
}
